export class SortColumnDirection {
  active?: string;
  direction: string;
}

export enum SortDirection {
  asc = 'asc',
  desc = 'desc'
}

export enum ItemActionType {
  add = 'add',
  delete = 'delete'
}
